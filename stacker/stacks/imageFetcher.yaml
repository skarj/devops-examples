sys_path: .
stacker_bucket: ${namespace}-stacker

stacks:
  - name: vpc
    class_path: blueprints.vpc.EKSVPC
    variables:
      Namespace: ${namespace}
      BaseCidr: "10.37"

  # - name: imagefetcher
  #   class_path: blueprints.imagefetcher.Imagefetcher
  #   variables:
  #     Namespace: ${namespace}
  #     GithubRepo: ${imagefetcherGithubRepo}
