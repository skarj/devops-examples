sys_path: .
stacker_bucket: ${namespace}-stacker

stacks:
  - name: imagefetcher
    class_path: blueprints.imagefetcher.Imagefetcher
    variables:
      Namespace: ${namespace}
      GithubRepo: ${imagefetcherGithubRepo}

  - name: ekscluster
    class_path: blueprints.eks_cluster.EKSCluster
    variables:
      Namespace: ${namespace}
